# Bitacora del incremento P02 - PR09 Monitoreo de huerto o ambiente

Formato (M02, "Bitacora minima"): fecha, version, comando, resultado, interpretacion, siguiente accion.
Hora local America/Mexico_City (UTC-6). Un resultado fallido se registra tal cual; no se borra historial.

| # | Fecha y hora | Version | Comando / accion | Resultado | Interpretacion | Siguiente accion |
|---|---|---|---|---|---|---|
| 1 | 2026-09-11 17:29 | - | Lectura de M02 y P02 (PDF). Revision de herramientas: `java -version`, `mvn -version`. | Java 11.0.32.1, Maven 3.9.9 presentes; Tomcat ausente; PostgreSQL 11 nativo en 5432 (credenciales desconocidas). | El modulo pide Tomcat 9.0.115 y PostgreSQL 16; hay que instalar/levantar ambos sin tocar el PG nativo ajeno. | Descargar Tomcat 9.0.115; usar PostgreSQL 16 en Docker con puerto propio (5436). |
| 2 | 2026-09-11 17:30 | Tomcat 9.0.115 | `curl -o apache-tomcat-9.0.115.zip https://archive.apache.org/dist/tomcat/tomcat-9/v9.0.115/bin/...` + `unzip` en `C:/Tools` | Tomcat desempaquetado (no se versiona). | Version exacta del paquete docente; evita mezclar `jakarta`. | Crear `bin/setenv.bat` con `DB_URL/DB_USER/DB_PASSWORD`. |
| 3 | 2026-09-11 17:33 | postgres:16 | `docker compose -f docker/docker-compose.yml up -d` | Contenedor `healthy`; log muestra `running 01_schema.sql` y `02_seed.sql`. | Esquema y semilla ficticia se crean solos la primera vez (reproducible). | Escribir codigo Java. |
| 4 | 2026-09-11 17:39 | web1 0.2.0 | `mvn -q clean package` | `Tests run: 10, Failures: 0`; `target/web1.war` (1.5 MB). | Pruebas unitarias en verde sin BD. | Desplegar en Tomcat. |
| 5 | 2026-09-11 17:39 | Tomcat 9.0.115 | `startup.bat` con `setenv.bat`; `curl /web1/health` | `Server startup in [1785] ms`; `/health` 200 `{"db":"UP","postgres":"PostgreSQL 16.15 ..."}`. | Conexion JDBC externalizada funciona. | Ejecutar protocolo HTTP. |
| 6 | 2026-09-11 17:40 | web1 0.2.0 | Protocolo `curl`: GET, POST 26.5, POST abc, POST vacio, POST 150, POST sensor 999, POST 38, GET, GET /alertas, GET ruta inexistente | 200 / 303 / 400 / 400 (2 errores) / 400 rango fisico / 400 "no existe" / 303 `alerta=ALTA` / 200 con filas nuevas / 200 con alerta / 404 controlado. | Flujo principal, validaciones y alerta funcionan de extremo a extremo. | Automatizar en `verify-module.sh` y capturar pantallas. |
| 7 | 2026-09-11 17:41 | capturas.py v1 | `python scripts/capturas.py` | Capturas generadas, pero el POST "valido" produjo `alerta=BAJA` con 27.5. | **Fallo diagnosticado:** el script elegia el sensor por posicion (`index=1`) y el primero del catalogo es humedad de suelo (umbral 40-80), no temperatura. | Seleccionar por codigo `SEN-A-TEMP-01`; poner cada `<option>` en una linea para leer su id desde el HTML. |
| 8 | 2026-09-11 17:42 | verify-module.sh v1 | `CATALINA_HOME=... ./scripts/verify-module.sh M02` | 5 VERIFICADO, 10 NO_VERIFICADO (GET inicial 404, resto en cascada). | **Fallo diagnosticado:** el script copiaba el WAR y consultaba de inmediato; Tomcat tarda unos segundos en replegar/desplegar (autoDeploy) y una peticion durante el repliegue devolvio 500 (NPE en el mapper de Tomcat, no en la app). Tomcat tambien avisaba que el driver JDBC no se anulaba al parar el contexto. | Esperar 404 tras retirar el WAR y 200 tras copiarlo; agregar `AppContextListener`. |
| 9 | 2026-09-11 17:44 | verify-module.sh v2 | `./scripts/verify-module.sh M02` | 14 VERIFICADO, 1 NO_VERIFICADO ("Persistencia en PostgreSQL"). | **Fallo diagnosticado:** las filas si estaban en la tabla, pero la consulta de verificacion no incluia `observacion` y el `grep` de la marca fallaba. | Agregar `l.observacion` a `sql/03_consultas_verificacion.sql`. |
| 10 | 2026-09-11 17:45 | verify-module.sh v3 | `./scripts/verify-module.sh M02` | **15/15 VERIFICADO**. | El incremento se reconstruye y comprueba con un solo comando. | Regenerar capturas, prueba negativa de infraestructura. |
| 11 | 2026-09-11 17:47 | web1 0.2.0 | `docker stop dsw-p02-huerto-db`; `curl /health`; `curl /lecturas`; `docker start ...` | `/health` 503 `{"status":"DOWN","error":"Connection ... refused"}`; `/lecturas` 500 con pagina de error controlada; tras `start`, `/health` 200. | La app degrada con mensaje util y sin exponer credenciales; se recupera sin reiniciar Tomcat. | Anonimizar rutas y documentar. |
| 12 | 2026-09-11 17:50 | - | `git init`, commits por componente | Historial creado; `target/`, `setenv.*` y `.env` excluidos. | Trazabilidad del codigo por commit. | Confirmar identificador PRxx con la ficha C11. |
| 13 | 2026-09-11 18:17 | C11 | Lectura de `C11_banco_proyectos_integradores.pdf` y de la consigna F02. | El proyecto es **PR09**. La ficha exige entidades `zona, variable, lectura, umbral, alerta, anotacion`, RF01-RF09 numerados, tres **roles funcionales** (responsable, observador, coordinacion) y el riesgo "presentar simulacion como medicion real". | **Desajuste diagnosticado:** el modelo tenia `huerto` y `tipo_sensor`, faltaba `anotacion`, los RF usaban otra numeracion y "tres roles" se habia interpretado como roles del equipo. | Alinear modelo, RF, roles y documentos a PR09 sin salir de 7 entidades. |
| 14 | 2026-09-11 18:26 | web1 0.2.0 (PR09) | Refactor: `tipo_sensor` -> `variable`, `huerto` eliminado, `anotacion` agregada, `AnotacionServlet` (/anotaciones), `AnotacionValidator` + 4 pruebas; `docker compose down -v && up -d`; `mvn clean package` | 7 tablas (conteos 2/4/5/5/2/0/1); `Tests run: 14, Failures: 0`; WAR 1.58 MB. | El modelo coincide con la ficha (6 entidades + `sensor` como extension justificada). | Ampliar `verify-module.sh` y `capturas.py` con anotaciones. |
| 15 | 2026-09-11 18:30 | verify-module.sh v4 | `./scripts/verify-module.sh M02` + `capturas.py` + `03_consultas_verificacion.sql` + prueba BD detenida + `anonimizar_evidencia.py` | **18/18 VERIFICADO**; 13 capturas; estado final con 2 lecturas manuales, 1 alerta ALTA y 2 anotaciones; `/health` 503 con BD detenida. | Evidencia completa y coherente con el codigo PR09. | Detectado que `02_pruebas_unitarias.txt` no se generaba (`cp` de dos reportes a un solo archivo); corregido con `cat`. |
| 16 | 2026-09-11 18:35 | - | Documentacion: F02 (comparacion PR09/PR03/PR01 y plantilla), acta con roles funcionales y RF de la ficha, modelo, trazabilidad, README, indice R02. `git commit`. | Repositorio sin `PRxx` ni rutas privadas. | Trazabilidad C11 -> F02 -> P02 completa. | `git push` a GitHub (`zywoxxx`) y registrar en F02 la respuesta del facilitador. |
| 17 | 2026-09-11 19:33 | PostgreSQL 17.11 nativo | `psql -p 5433`: `CREATE ROLE huerto_app`, `CREATE DATABASE huerto_db`, `01_schema.sql`, `02_seed.sql`, `03_consultas_verificacion.sql`; Tomcat con `DB_URL` al puerto 5433; `curl /health`, POST 29.5, POST abc, `psql` | 7 tablas y semilla en el servidor nativo; `/health` reporta PostgreSQL 17.11; POST 303 (lectura 3 persistida), POST abc 400. Primer intento fallido: `shutdown.bat` no alcanzo a detener Tomcat y el POST cayo en Docker; se borro ese registro y se repitio esperando a que el puerto 8080 quedara libre. | La Opcion B del README (PostgreSQL instalado) queda VERIFICADA; el WAR no cambia, solo la variable de entorno. | Restaurar Tomcat a Docker (5436) y documentar. |

## Pruebas pendientes o no verificadas (declaracion honesta)

- **PENDIENTE**: ejecucion completa por otro integrante en una maquina limpia (Linux o Windows) siguiendo solo el README (CA05).
- **PENDIENTE**: registrar en `docs/F02-seleccion-proyecto.md` la fecha y texto de la confirmacion del facilitador en F02.
- **NO_VERIFICADO**: despliegue en Linux/macOS con `setenv.sh` (solo se probo Windows con `setenv.bat`).
- **NO_VERIFICADO por HTTP**: valor con tres decimales y sensor inactivo (cubiertos solo por pruebas unitarias; la semilla no incluye sensores inactivos).
- **NO_VERIFICADO**: `/health` con variables `DB_*` ausentes (se verifico "base detenida", no "sin configuracion").
- **NO_VERIFICADO**: anotacion asociada a una lectura inexistente por HTTP (regla implementada en `AnotacionServlet`, sin caso en el protocolo).
- **PENDIENTE**: publicacion del repositorio en GitHub (requiere credenciales del integrante).
