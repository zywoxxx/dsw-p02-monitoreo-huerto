# Bitacora del incremento P02 - PRxx Monitoreo de huerto

Formato (M02, "Bitacora minima"): fecha, version, comando, resultado, interpretacion, siguiente accion.
Hora local America/Mexico_City (UTC-6). Un resultado fallido se registra tal cual; no se borra historial.

| # | Fecha y hora | Version | Comando / accion | Resultado | Interpretacion | Siguiente accion |
|---|---|---|---|---|---|---|
| 1 | 2026-09-11 17:29 | - | Lectura de M02 y P02 (PDF). Revision de herramientas: `java -version`, `mvn -version`. | Java 11.0.32.1, Maven 3.9.9 presentes; Tomcat ausente; PostgreSQL 11 nativo en 5432 (credenciales desconocidas). | El modulo pide Tomcat 9.0.115 y PostgreSQL 16; hay que instalar/levantar ambos sin tocar el PG nativo ajeno. | Descargar Tomcat 9.0.115; usar PostgreSQL 16 en Docker con puerto propio (5436). |
| 2 | 2026-09-11 17:30 | Tomcat 9.0.115 | `curl -o apache-tomcat-9.0.115.zip https://archive.apache.org/dist/tomcat/tomcat-9/v9.0.115/bin/...` + `unzip` en `C:/Tools` | Tomcat desempaquetado (no se versiona). | Version exacta del paquete docente; evita mezclar `jakarta`. | Crear `bin/setenv.bat` con `DB_URL/DB_USER/DB_PASSWORD`. |
| 3 | 2026-09-11 17:33 | postgres:16 | `docker compose -f docker/docker-compose.yml up -d` | Contenedor `healthy`; log muestra `running 01_schema.sql` y `02_seed.sql`. Conteos: huerto 1, zona 2, tipo_sensor 4, sensor 5, umbral 5, lectura 2, alerta 0. | Esquema de 7 tablas y semilla ficticia se crean solos la primera vez (reproducible). | Escribir codigo Java. |
| 4 | 2026-09-11 17:39 | web1 0.2.0 | `mvn -q clean package` | `Tests run: 10, Failures: 0`; `target/web1.war` (1.5 MB) con classes, JSTL 1.2, postgresql 42.7.2, web.xml, vistas. | Pruebas unitarias de RF-03/RF-05 en verde sin BD. | Desplegar en Tomcat. |
| 5 | 2026-09-11 17:39 | Tomcat 9.0.115 | `startup.bat` con `setenv.bat`; `curl /web1/health` | `Server startup in [1785] ms`; `/health` 200 `{"db":"UP","postgres":"PostgreSQL 16.15 ..."}`. | Conexion JDBC externalizada funciona. | Ejecutar protocolo HTTP. |
| 6 | 2026-09-11 17:40 | web1 0.2.0 | Protocolo `curl`: GET, POST 26.5, POST abc, POST vacio, POST 150, POST sensor 999, POST 38, GET, GET /alertas, GET ruta inexistente | 200 / 303 `creada=3` / 400 / 400 (2 errores) / 400 rango fisico / 400 "no existe" / 303 `alerta=ALTA` / 200 con filas nuevas / 200 con alerta / 404 controlado. | Flujo principal, validaciones y alerta funcionan de extremo a extremo. | Automatizar en `verify-module.sh` y capturar pantallas. |
| 7 | 2026-09-11 17:41 | capturas.py v1 | `python scripts/capturas.py` | Capturas generadas, pero el POST "valido" produjo `alerta=BAJA` con 27.5. | **Fallo diagnosticado:** el script elegia el sensor por posicion (`index=1`) y el primero del catalogo es humedad de suelo (umbral 40-80), no temperatura. | Seleccionar por codigo `SEN-A-TEMP-01`; poner cada `<option>` en una linea para poder leer su id desde el HTML. |
| 8 | 2026-09-11 17:42 | verify-module.sh v1 | `CATALINA_HOME=... ./scripts/verify-module.sh M02` | 5 VERIFICADO, 10 NO_VERIFICADO (GET inicial 404, resto en cascada). | **Fallo diagnosticado:** el script copiaba el WAR y consultaba de inmediato; Tomcat tarda unos segundos en replegar/desplegar (autoDeploy) y una peticion durante el repliegue devolvio 500 (NPE en el mapper de Tomcat, no en la app). Ademas Tomcat avisaba que el driver JDBC no se anulaba al parar el contexto. | Esperar 404 tras retirar el WAR y 200 tras copiarlo; agregar `AppContextListener` que anula el registro del driver. |
| 9 | 2026-09-11 17:44 | verify-module.sh v2 | `./scripts/verify-module.sh M02` | 14 VERIFICADO, 1 NO_VERIFICADO ("Persistencia en PostgreSQL"). | **Fallo diagnosticado:** las filas si estaban en la tabla (`lectura` = 4, `alerta` = 1), pero la consulta de verificacion no incluia `observacion`, asi que el `grep` de la marca fallaba. | Agregar `l.observacion` a `sql/03_consultas_verificacion.sql`. |
| 10 | 2026-09-11 17:45 | verify-module.sh v3 | `./scripts/verify-module.sh M02` (salida completa en `docs/evidencia/txt/verify-module_salida.txt`) | **15/15 VERIFICADO**, `RESULTADO: VERIFICADO`. | El incremento se reconstruye y comprueba desde cero con un solo comando. | Regenerar capturas y guardar estado final de BD. |
| 11 | 2026-09-11 17:45 | capturas.py v2 | `sql/99_cleanup.sql` + `python scripts/capturas.py` + `03_consultas_verificacion.sql` | 10 capturas en `docs/evidencia/img/`; dos lecturas nuevas (27.5 C en rango y 38 C con alerta ALTA); estado final en `txt/17_postgres_estado_final.txt`. | Las capturas corresponden a filas verificables en PostgreSQL. | Prueba negativa de infraestructura. |
| 12 | 2026-09-11 17:47 | web1 0.2.0 | `docker stop dsw-p02-huerto-db`; `curl /health`; `curl /lecturas`; `docker start ...` | `/health` 503 `{"status":"DOWN","error":"Connection ... refused"}`; `/lecturas` 500 con pagina de error controlada; tras `start`, `/health` 200 de nuevo. | La app degrada con mensaje util y sin exponer credenciales; se recupera sin reiniciar Tomcat. | Anonimizar rutas y documentar. |
| 13 | 2026-09-11 17:48 | - | `python scripts/anonimizar_evidencia.py` | Rutas locales sustituidas por `<REPO>`, `<CATALINA_HOME>`, `<HOME>` en `docs/evidencia/txt`. | Cumple "sin rutas privadas" de P02. | README, indice de evidencia, commits. |
| 14 | 2026-09-11 17:50 | - | `git init`, commits por componente | Historial creado; `target/`, `setenv.*` y `.env` excluidos. | Trazabilidad del codigo por commit. | `git push` a GitHub (`zywoxxx`) por el integrante con sesion iniciada. |

## Pruebas pendientes o no verificadas (declaracion honesta)

- **PENDIENTE**: ejecucion completa por otro integrante en una maquina limpia (Linux o Windows) siguiendo solo el README.
- **NO_VERIFICADO**: despliegue en Linux/macOS con `setenv.sh` (solo se probo Windows con `setenv.bat`).
- **NO_VERIFICADO por HTTP**: valor con tres decimales y sensor inactivo (cubiertos solo por pruebas unitarias; la semilla no incluye sensores inactivos).
- **NO_VERIFICADO**: `/health` con variables `DB_*` ausentes (se verifico el caso "base detenida", no el caso "sin configuracion").
- **PENDIENTE**: publicacion del repositorio en GitHub (requiere credenciales del integrante).
