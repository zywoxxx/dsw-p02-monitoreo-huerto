# Copiar a docker/.env solo si se desea cambiar los valores de laboratorio.
# NO versionar docker/.env (esta en .gitignore).
POSTGRES_DB=huerto_db
POSTGRES_USER=huerto_app
POSTGRES_PASSWORD=huerto_dev
POSTGRES_PORT=5436
